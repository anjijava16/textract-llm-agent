[project]
name = "api-service-textract"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
# Web framework
"fastapi", #= "^0.110.0"
"uvicorn", # = { extras = ["standard"], version = "^0.29.0" }

# AWS SDK
"boto3",# = "^1.34.0"

# Utilities
"python-multipart",# = "^0.0.9"   # required for file upload
"pydantic",# = "^2.6.0"

]
